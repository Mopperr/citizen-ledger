# ─────────────────────────────────────────────────────────────────────────────
# Citizen Ledger Testnet Faucet - Configuration
# ─────────────────────────────────────────────────────────────────────────────

# Chain Configuration
RPC_ENDPOINT=http://localhost:26657
CHAIN_ID=citizen-testnet-1
DENOM=ucitizen
BECH32_PREFIX=citizen
GAS_PRICE=0.025ucitizen

# Faucet Wallet (REQUIRED)
# Generate with: wasmd keys add faucet --keyring-backend test
# Then show mnemonic: wasmd keys show faucet --keyring-backend test --output json
FAUCET_MNEMONIC="your twenty four word mnemonic phrase goes here word word word word word word word word word word word word"

# Faucet Settings
AMOUNT_PER_REQUEST=10000000000  # 10,000 CITIZEN (in ucitizen)
PORT=3001

# Rate Limiting
RATE_LIMIT_WINDOW=86400   # 24 hours in seconds
RATE_LIMIT_MAX=1          # 1 request per address per window
